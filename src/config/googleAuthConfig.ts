// Web Client ID from the Google Cloud Console credentials page
// (APIs & Services > Credentials > OAuth 2.0 Client IDs > "Web client").
// This is required even for Android/iOS sign-in — it's how Google issues
// an idToken your app can trust. Replace the placeholder below before
// building; Google Sign-In will fail with DEVELOPER_ERROR until it's set.
export const GOOGLE_WEB_CLIENT_ID = '744284318253-0347mp7rag53ohobivfieorukunsj5c9.apps.googleusercontent.com';